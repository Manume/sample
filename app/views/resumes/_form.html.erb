<%= form_for @resume, html: { class: "form-horizontal", style: "display:inline;" } do |f| %>
  <div class="modal-body">
    <center>
      <% if @resume.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@resume.errors.count, "error") %> prohibited this resume from being saved:</h2>

        <ul>
        <% @resume.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    </center>


      <div class="form-group">
          <%= f.label :full_name, class:"col-sm-4 control-label" %>
          <div class="col-sm-8">
            <%= f.text_field :full_name,class: "form-control" %>
          </div>
      </div>
      <div class="form-group">
          <%= f.label :email_address, class:"col-sm-4 control-label" %>
          <div class="col-sm-8">
            <%= f.text_field :email_address,class: "form-control" %>
          </div>
      </div>
      <div class="form-group">
          <%= f.label :upload_cv, class:"col-sm-4 control-label" %>
          <div class="col-sm-8">
            <%= f.file_field :upload_cv %>
          </div>
      </div>
  </div>

  
  <div class="form-group">
    <div class="col-sm-offset-4 col-sm-8">
      <%= f.submit "Send apllication",class: "btn btn-info" %>
    </div>
  </div>
    
    
  
  
  
  
 
<% end %>

